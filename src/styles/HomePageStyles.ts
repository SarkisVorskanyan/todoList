import {makeStyles} from '@mui/styles'


export const useStyles = makeStyles({
    container: {
        display: 'flex',
        justifyContent: 'center',
        minHeight: '100%',
        marginTop: 100,
        padding: 10
    },
    header: {
        display: 'flex'

    }
})